name: Manual
on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "deploy"
